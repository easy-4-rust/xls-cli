interface:
  display_name: "xls-cli"
  short_description: "Safely automate local spreadsheet workflows"
  default_prompt: "Use $xls-cli to discover runtime capabilities, complete the spreadsheet task with structured JSON, dry-run every write, preserve warnings, and reopen outputs for verification."
